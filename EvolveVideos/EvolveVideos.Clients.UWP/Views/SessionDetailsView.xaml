<local:BasePage
    x:Class="EvolveVideos.Clients.UWP.Views.SessionDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EvolveVideos.Clients.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding SessionDetailsViewModel, Source={StaticResource ViewModelLocator}}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer Padding="52">
            <StackPanel>
                <TextBlock Text="{Binding Session.Title}" TextWrapping="Wrap" />
                <TextBlock Text="{Binding Session.Author}" TextWrapping="Wrap" />
                <TextBlock Text="{Binding Session.Track}" TextWrapping="Wrap" />
                <Image Source="{Binding Session.Thumbnail}" />
                <TextBlock Text="{Binding Session.Description}" TextWrapping="Wrap" />
                <Button Content="Play" Command="{Binding PlayCommand}" />
                <Button Content="Download" Command="{Binding DownloadCommand}" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</local:BasePage>